{
  "name": "nepal-disaster-update-live",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nepal Disaster Update Live — static site plus Cloudflare Pages Functions.",
  "scripts": {
    "build": "node src/build.mjs",
    "check": "node src/check.mjs",
    "test": "node --test test/*.test.mjs",
    "dev": "npx wrangler pages dev . --port 8788",
    "deploy": "./deploy.sh",
    "ping": "node src/indexnow.mjs"
  },
  "optionalDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
